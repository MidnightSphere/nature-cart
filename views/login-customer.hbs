<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Nature Cart</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background: #f0fdf4;
        }

        .login-container {
            max-width: 400px;
            margin: 50px auto;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        .form-control {
            border-radius: 5px;
        }

        .btn-custom {
            background: #05b160;
            color: white;
            width: 100%;
        }

        .btn-custom:hover {
            background: #048d4e;
        }

        .btn-back {
            background: #ccc;
            color: black;
            width: 55%;
        }
        .btn-back:hover {
            background: #bbb;
        }
    </style>
</head>

<body>

    <div class="login-container">
        <h3 class="text-center">Log in as Customer</h3>

            {{#if success}}
                  <div class="alert alert-success">{{success}}</div>
            {{/if}}

        <form id="login-form" action="/login-customer" method="post">
            <div class="mb-3">
                <label for="username" class="form-label">Username</label>
                <input type="text" class="form-control" id="username" name="username" placeholder="Enter your username" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input type="password" class="form-control" id="password" name="password" placeholder="Enter your password" required>
            </div>
            <button type="submit" class="btn btn-custom">Login</button>
        </form>

        <p class="text-center mt-3 text-muted">
            New User? <a href="./signup-customer">Create an account</a></p>
        </p>
        <button class="btn btn-back mt-3" onclick="window.location.href='/'">Go to Home Page</button>
    </div>

    <script>
    const errorMessage = "{{error}}";
    if (errorMessage) {
        alert(errorMessage);
    }
</script>

</body>

</html>